import { IsUserAuthenticatedHandler } from './is-user-authenticated.handler';

describe('IsUserAuthenticatedHandler', () => {
  it('should be defined', () => {
    expect(new IsUserAuthenticatedHandler()).toBeDefined();
  });
});
