import { IsUserAuthenticatedQuery } from './is-user-authenticated.query';

describe('IsUserAuthenticatedQuery', () => {
  it('should be defined', () => {
    expect(new IsUserAuthenticatedQuery()).toBeDefined();
  });
});
